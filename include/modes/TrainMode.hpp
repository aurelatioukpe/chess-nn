#ifndef MODES_TRAIN_MODE_HPP
#define MODES_TRAIN_MODE_HPP

#include <string>

int trainMode(const std::string& networkFile, const std::string& chessFile, 
              const std::string& saveFile);

#endif
